if OS.mac?
  # taps
  tap 'homebrew/cask'
  tap 'homebrew/cask-fonts'

  # Fonts
  cask 'font-hack-nerd-font'
end

tap 'homebrew/bundle'
tap 'homebrew/core'

brew 'gnupg'
brew 'helm'
brew 'k9s'
brew 'tmux'
brew 'asdf'